import { PequisaClientesPipePipe } from './pequisa-clientes-pipe.pipe';

describe('PequisaClientesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PequisaClientesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
