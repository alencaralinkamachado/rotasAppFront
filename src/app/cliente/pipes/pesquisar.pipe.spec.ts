import { PipePesquisarPipe } from './pesquisar.pipe';

describe('PipePesquisarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePesquisarPipe();
    expect(pipe).toBeTruthy();
  });
});
